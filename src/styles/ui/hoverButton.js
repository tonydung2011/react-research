import colors from '../colors';

export default {
    add: {
        button: {
            background: colors.red,
            color: 'white',
            padding: '10px',
            boxShadow: `0 3px 5px 2px ${colors.shadow}`,
            borderRadius: '50%',
            position: 'absolute',
            bottom: 20,
            right: 20,
        },
    }
}
